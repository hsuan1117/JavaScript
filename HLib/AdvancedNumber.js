class AdvanceNumber extends HLib {
  get sigma() {
    
  }
  get average() {
    [0, 1, 2, 3, 4].reduce((p, curr) => prev + curr )
  }
  constructor(data = [] , settings) {
    //存放計算的數組
    this.data = data;
    
  }
}
